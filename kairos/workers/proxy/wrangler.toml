name = "kairos-proxy"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[build]
command = ""

# Routes (à configurer après déploiement sur Cloudflare)
# routes = [{ pattern = "proxy.kairos.ch/*", zone_name = "kairos.ch" }]

# Variables d'environnement (non-secrètes)
[vars]
JOBROOM_API_BASE = "https://www.job-room.ch/api"
CACHE_TTL = "300"

# Development
[dev]
port = 8787
